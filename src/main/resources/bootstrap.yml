#region [Eureka Client Config]
#spring:
#  application:
#    name: 'scheduler-service'
#
#server:
#  port: 8083
#
#eureka:
#  instance:
##    hostname: ${vcap.application.uris[0]:localhost}
#    hostname: ${vcap.application.uris[0]:localhost}id:${spring.application.name}:${spring.application.instance_id:${random.value}}
#    prefer-ip-address: true
#    lease-renewal-interval-in-seconds: 5
#    lease-expiration-duration-in-seconds: 10 #works, just takes some time
#  client:
#    service-url:
#      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
#
#    fetch-registry: true

#endregion [Eureka Client Config]